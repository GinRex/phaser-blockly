import 'phaser';

class boot extends Phaser.Scene {
  constructor(props) {
    super({
      key: 'boot',
    });
  }
  preload() {
    // launch scene start
    // launch scene end
  }
  create() {
    // game create
  }
}

export default boot;
